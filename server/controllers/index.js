const stream = require("stream");
const path = require("path");
const dictionary = path.join(__dirname, "../", "dictionary.txt");

module.exports = {
  getTerm: (req, res) => {
    let body = "";
  }
};
